#include<stdio.h>
#include<string.h>
int main ()
{
	int i;
	char a[100];
	printf("string: ");
	scanf("%s",&a);
	 if(a[0]=='a'&&a[strlen(a)-1]=='a')
	 printf("accepted");
	 else
	 printf("not accepted");
	return 0;
}
